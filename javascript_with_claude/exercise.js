// Complete the code to create a function called addNumbers that takes two numbers as parameters and returns their sum.

function addNumbers(num1, num2) {
  // TODO: Declare a variable called sum
  let sum;
  // TODO: Add num1 and num2 and assign the result to sum
  sum = num1 + num2;
  // TODO: Return the sum variable
  return sum;
}

// Call the function with sample parameters and log the result
let result = addNumbers(5, 3);
console.log(result);

// The completed function should add the two numbers passed to it and return the result
